<div class="container p-4">
    <div class="row">
        <div class="container">
            {{#each seccion}}
            <h3>Votantes de la Sección {{secc}} - Casilla {{cass}}</h3>
            {{else}}
            <h3>Votantes de la Sección ### - Casilla ###</h3>
            {{/each}} 
            <br>
            <div class="container inner" style="display: flex; align-items: center; justify-content: center;">
                <div class='inner alert alert-success alert-dismissible fade' role='alert'>¡Voto Registrado!<button type='button' class='btn-close' data-bs-dismiss='alert' aria-label='Close'></button></div>
                <div class='inner2 alert alert-danger alert-dismissible fade' role='alert'>Algo salió mal. Intenta nuevamente.<button type='button' class='btn-close' data-bs-dismiss='alert' aria-label='Close'></button></div>

            </div>
        </div>
        <div class="table-wrapper-scroll-y my-custom-scrollbar" style="height: 80vh;">
            <table id="example" class="table table-bordered table-striped mb-0" style="width: 100%;">
                <thead>
                    <tr>
                        <th class="idee" style="display: none;" scope="col">Id</th>
                        <th scope="col"># Lista</th>
                        <th scope="col">Nombre</th>
                        
                        <td scope="col">Votó</td>
                    </tr>
                </thead>
                <tbody>
                    {{#each votantes}}
                    <tr>
                        <td style="display: none;" class="id">{{id}}</td>
                        <td>{{num_lista_nominal}}</td>
                        <td>{{ape_pat}} {{ape_mal}} {{nombres}}</td>

                        <td class="text-center"><button class="vota btn btn-primary" id="vota"><i class="fas fa-vote-yea"></i></button></td>
                    </tr>
                    {{else}}
                    <tr>
                        <td>Sin Datos</td>
                        <td>Sin Datos</td>
                        <td>Sin Datos</td>
                        <td>Sin Datos</td>
                    </tr>
                    {{/each}}
                    
                </tbody>
            </table>

        </div>
        <style>
            .my-custom-scrollbar {
                position: relative;
                height: 200px;
                overflow: auto;
            }
            .id,.idee{
                display: none;
            }
            .table-wrapper-scroll-y {
                display: block;
            }
        </style>
    </div>
</div>

<div class="modal" id="exampleModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Alerta</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        ¿Seguro que desea registrar el voto?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
        <button type="button" id="aceptar" class="aceptar btn btn-primary">Guardar Voto</button>
      </div>
    </div>
  </div>
</div>